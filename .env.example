# Server
PORT=3000

# Google Maps JavaScript API key (restrict to your prod domain in GCP)
GOOGLE_MAPS_API_KEY=your_public_maps_js_key

# Database
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DB?sslmode=require

# Privacy (optional): salt used to hash IPs if you choose to store hashed IPs
IP_HASH_SALT=change_me